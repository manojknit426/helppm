<%if @law.errors.any?%>

$("#title_error").text("* title already taken");

<%else%>
$('#rule').val('');
$('#submit').val('add another rule');
v=$("#title").val();

$("#title").val(parseInt(v)+1);
$("#title_error").text("");
$("#heading").val("");
<%end%>
